# This file ensures the secrets directory is tracked by git
# Add your actual secret files here (they should be .gitignored)

# Required secret files:
# - secret_key.txt
# - jwt_private_key.pem
# - db_encryption_key.txt
# - db_password.txt
# - redis_password.txt
# - ip_hash_salt.txt
# - aws_access_key_id.txt
# - aws_secret_access_key.txt

# Generate secure secrets:
# openssl rand -hex 32 > secret_key.txt
# openssl genrsa -out jwt_private_key.pem 2048
# openssl rand -hex 32 > db_encryption_key.txt
